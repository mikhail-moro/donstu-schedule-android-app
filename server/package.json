{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "googleapis": "^103.0.0",
    "selenium": "^2.20.0",
    "selenium-webdriver": "^4.2.0"
  },
  "name": "t-uni_parse",
  "version": "1.0.0",
  "description": "Listen responses from app, and execute script with fetch in browser console, then return json-file with resulst of fetch if was Get response",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "server",
    "heroku"
  ],
  "author": "mikhail_moro",
  "license": "MIT",
  "engines": {
    "node": "v18.1.0"
  }
}
